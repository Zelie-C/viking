import { Character } from "../Character/Character";
import { SpellBook } from "./SpellBook";

export class Spells extends SpellBook {
    public cast(character: Character){
    }

    public canCast(character: Character){
    }
}